sr sum(num1, num2)
{
    return num1 + num2;
}

sr arrays()
{
    var ar = ["Hello", "World", "Vector", "of", "Vectors"];

    for (var i = 0; i < ar.len; i++)
        pout(ar[i]);


    rm(ar); 
}

sr string_access()
{
    var ar = ["Hello", "World", "Vector", "of", "Vectors"];
    var str = "aaron dykes is really coooooool Hhhhhhhh";


    for (var i = 0; i < str.len; i++)
        pout(str[i]);

    pout(str, ar); 


    rm(ar); 
}

arrays();
string_access();