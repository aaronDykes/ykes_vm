class NP
{

    print_prime(n)
    {
        for (var i = 3; i < n; i++)
            if (prime(i))
                pout(i);
    }

    res()
    {
        var start = clock();
        this.print_prime(100000);
        pout(clock() - start);
    }
    
}


