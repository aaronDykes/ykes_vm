sr pr()
{

    sr print_prime(n)
    {
        for (var i = 3; i < n; i++)
            if (prime(i))
                pout(i);
    }

    var start = clock();
    print_prime(10000000);
    pout(clock() - start);
}

